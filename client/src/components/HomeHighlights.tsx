import React, { useEffect, useRef } from 'react';
import { homeShowcaseSections } from '../config/siteConfig';

const HomeHighlights = () => {
  // Temporarily hide the BEAUTIFUL SPACES section
  return null;
};

export default HomeHighlights;